import 'package:flutter/material.dart';

class PsychometricQuizScreen extends StatelessWidget {
  const PsychometricQuizScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Psychometric Quiz')),
      body: const Center(child: Text('Psychometric Quiz')),
    );
  }
}
