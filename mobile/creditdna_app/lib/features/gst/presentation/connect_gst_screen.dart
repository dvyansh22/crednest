import 'package:flutter/material.dart';

class ConnectGstScreen extends StatelessWidget {
  const ConnectGstScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Connect GST')),
      body: const Center(child: Text('Connect GST')),
    );
  }
}
